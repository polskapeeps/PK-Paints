# Copy to .env and fill in securely. Do not commit .env.
# Vercel rollback configuration. For Cloudflare use .dev.vars locally and Worker secrets remotely.
# Never prefix server secrets with VITE_; that exposes them in browser assets.
RESEND_API_KEY=
# Must use a sending domain verified in the connected Resend account.
ESTIMATE_FROM_EMAIL=PK Paints Website <forms@send.pkpaintsrenovations.com>
ESTIMATE_TO_EMAIL=pkpaintsreno@gmail.com
